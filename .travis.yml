matrix:
   include:
    #######################
    #    Python Matrix    #
    #######################
     - language: python
       dist: xenial
       python: 
         - "3.6"
         - "3.7"
       install:
         - pip install -r requirements.txt
         - pip install -r requirements-ci.txt
         - bash setup.sh 
       script:
          - snekcheck
    #######################
    #    Node.js Matrix   #
    #######################
     - language: node_js
       dist: xenial
       node_js:
         - "8"
         - "10"
       install:
        - cd services
        - npm i --save
        - npm ci 
        - bash setup.sh 
       script:
         - echo "No tests for now"
         

notifications:
  email: false

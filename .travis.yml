language: python

python:
  - "3.6"
  - "3.7"

install:
  - "pip install -r requirements.txt"
  - "pip install -r requirements-ci.txt"
  - bash setup.sh

script:
  - echo "No tests"

cache: pip

notifications:
  email: false
